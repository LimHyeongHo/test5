# QA 테스트 결과 리포트 🚨

프론트엔드와 백엔드 서버를 띄우고 브라우저 테스트 봇을 투입하여 테스트를 진행한 결과, **로그인 단계에서 치명적인 문제점**이 발견되어 테스트가 중단되었습니다.

## 🐛 발견된 문제점

### 1. 테스트 계정 DB 누락 (로그인 불가)
`LoginPage.jsx`의 "관리자 로그인" 탭에 적혀있는 테스트 계정(`seller01@test.com` 등)으로 로그인을 시도했으나 서버에서 거부(500 에러)되었습니다.
- **원인:** 백엔드 코드(`PkiController.java`)에는 해당 이메일들이 하드코딩되어 접근을 허용하고 있지만, **실제 연결된 DB(Supabase)에는 해당 계정 데이터가 존재하지 않아** 조회가 불가능했습니다.

### 2. 프론트엔드 로그인 페이지 무한 새로고침 버그
로그인 실패 시 사용자 경험(UX)에 문제가 있습니다.
- **현상:** DB에 없는 계정으로 로그인 시 서버가 에러를 반환하면, 프론트엔드(`catch` 블록)에서 `alert("로그인 오류...")`를 띄운 직후 `window.location.reload()`를 강제로 실행합니다.
- **결과:** 에러를 확인하기도 전에 페이지가 휙휙 새로고침되어 버리거나 봇(Bot) 테스트 환경에서 무한 루프에 빠지는 원인이 됩니다.

---

## 🛠️ 제안하는 해결 방안

이 문제를 해결하고 테스트를 완료하기 위해 다음 작업을 수행하는 것을 추천합니다.

1. **백엔드 테스트 계정 자동 생성 (DataInitializer):** 
   - 서버가 켜질 때 DB를 확인해서 `seller01@test.com` 등 필수 테스트 계정이 없으면 자동으로 생성해주는 코드를 백엔드에 추가합니다.
2. **프론트엔드 에러 처리 개선:** 
   - `LoginPage.jsx`에서 로그인 실패 시 강제로 새로고침(`reload()`)하는 대신, 화면에 에러 메시지만 남기도록 코드를 수정합니다.

이 두 가지를 제가 바로 고쳐드릴까요?
